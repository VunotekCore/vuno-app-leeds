CREATE DATABASE IF NOT EXISTS vuno_app_leed
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE vuno_app_leed;

CREATE TABLE IF NOT EXISTS users (
  id BINARY(16) PRIMARY KEY,
  email VARCHAR(255) NOT NULL UNIQUE,
  password VARCHAR(255) NOT NULL,
  whatsapp_apikey VARCHAR(255) DEFAULT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB;

CREATE TABLE IF NOT EXISTS tiers (
  id BINARY(16) PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  price DECIMAL(10,2) NOT NULL,
  created_by BINARY(16) NOT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB;

CREATE TABLE IF NOT EXISTS templates (
  id BINARY(16) PRIMARY KEY,
  template_name VARCHAR(255) NOT NULL,
  message_body TEXT NOT NULL,
  created_by BINARY(16) NOT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB;

CREATE TABLE IF NOT EXISTS leads (
  id BINARY(16) PRIMARY KEY,
  store_name VARCHAR(255) NOT NULL,
  profile_url VARCHAR(500) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  email VARCHAR(255) DEFAULT NULL,
  followers_count INT DEFAULT 0,
  tier_classification VARCHAR(50) DEFAULT NULL,
  contact_status ENUM('Pending', 'First Contact', 'Second Contact', 'Interested', 'Client', 'Archived') NOT NULL DEFAULT 'Pending',
  contact_attempts INT NOT NULL DEFAULT 0,
  last_contact_date DATETIME DEFAULT NULL,
  created_by BINARY(16) NOT NULL,
  registration_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE CASCADE,
  UNIQUE KEY uq_phone_profile (phone, profile_url),
  INDEX idx_follow_up (contact_status, last_contact_date)
) ENGINE=InnoDB;

INSERT INTO users (id, email, password, created_at) VALUES
  (UUID_TO_BIN('018e7c40-0000-7000-8000-000000000001'), 'dail', '$2y$10$kyuPfiPWb9iKduUb15wPL.ReNE7KZpkkB8SPGIRCgE4tufa7CfXX.', NOW())
ON DUPLICATE KEY UPDATE email = email;

INSERT INTO tiers (id, name, price, created_by) VALUES
  (UUID_TO_BIN('01900001-0000-7000-8000-000000000001'), 'Tier 1', 50.00, UUID_TO_BIN('018e7c40-0000-7000-8000-000000000001')),
  (UUID_TO_BIN('01900001-0000-7000-8000-000000000002'), 'Tier 2', 100.00, UUID_TO_BIN('018e7c40-0000-7000-8000-000000000001')),
  (UUID_TO_BIN('01900001-0000-7000-8000-000000000003'), 'Tier 3', 200.00, UUID_TO_BIN('018e7c40-0000-7000-8000-000000000001'))
ON DUPLICATE KEY UPDATE name = name;
